import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const NomineeList = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>NomineeList</Text>
    </View>
  );
};

export default NomineeList;

const styles = StyleSheet.create({});
